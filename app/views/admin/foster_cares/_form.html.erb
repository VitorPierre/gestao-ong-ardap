<%= form_with(model: [:admin, foster_care], class: "contents") do |form| %>
  <% if foster_care.errors.any? %>
    <div id="error_explanation" class="bg-red-50 text-red-500 px-3 py-2 font-medium rounded-md mt-3">
      <h2><%= pluralize(foster_care.errors.count, "error") %> prohibited this foster_care from being saved:</h2>

      <ul class="list-disc ml-6">
        <% foster_care.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="inline">
    <%= form.submit class: "w-full sm:w-auto rounded-md px-3.5 py-2.5 bg-blue-600 hover:bg-blue-500 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
